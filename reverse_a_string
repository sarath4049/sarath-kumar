#include<stdio.h>

int main()
{
  char sent[500]="hi ra";
  char rev[500];
  int i,len=0;
  for(i=0;sent[i]!='\0';i++)
  {
      len+=1;
  }
  for(i=0;i<l;i++)
  {
    rev[i]=sent[l-i];
  }
  for(i=0;i<l;i++)
  {
    printf("%c",rev[i]);
  }
  return 0;
}
